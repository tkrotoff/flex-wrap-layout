{
  "name": "flex-wrap-layout",
  "description": "Experiments with flexbox layout",
  "repository": {
    "type" : "git",
    "url" : "https://github.com/tkrotoff/flex-wrap-layout.git"
  },
  "version": "0.0.1",
  "license": "MIT",

  "main": "lib/index.js",
  "types": "lib/index.d.ts",

  "scripts": {
    "clean": "del-cli build lib dist/*.js",

    "dist": "webpack --config webpack.dist.config.ts",

    "build": "webpack -d",
    "build:watch": "webpack -d --watch",

    "tsc": "tsc",
    "prepublishOnly": "npm run clean && npm run build && npm run tsc && npm run dist",

    "test": "npm run prepublishOnly",

    "tslint": "tslint --config tslint.json '**/*.ts{,x}' --exclude '**/node_modules/**' --exclude 'lib/**'"
  },

  "devDependencies": {
    "typescript": "latest",
    "tslint": "latest",
    "tslint-react": "latest",

    "react": "latest",
    "@types/react": "latest",
    "react-dom": "latest",
    "@types/react-dom": "latest",

    "node-sass": "latest",

    "bootstrap-sass": "^3.3",

    "ts-node": "latest",
    "@types/node": "latest",

    "webpack": "latest",
    "@types/webpack": "latest",
    "ts-loader": "latest",
    "style-loader": "latest",
    "css-loader": "latest",
    "sass-loader": "latest",
    "file-loader": "latest",

    "del-cli": "latest"
  }
}
