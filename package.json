{
  "name": "flexform",
  "description": "Experiments with flexbox layout",
  "repository": {
    "type" : "git",
    "url" : "https://github.com/tkrotoff/flexform.git"
  },
  "version": "0.0.1",
  "license": "MIT",

  "scripts": {
    "clean": "del-cli build",
    "build": "webpack -d --env.development",
    "build:watch": "webpack -d --env.development --watch",
    "test": "npm run build"
  },

  "dependencies": {
    "node-sass": ">=3.4.2"
  },

  "devDependencies": {
    "typescript": "latest",
    "tslint": "latest",
    "ts-node": "latest",

    "bootstrap-sass": "^3.3",

    "webpack": "latest",
    "@types/webpack": "latest",
    "ts-loader": "latest",
    "style-loader": "latest",
    "css-loader": "latest",
    "sass-loader": "latest",
    "file-loader": "latest",

    "del-cli": "latest"
  }
}
